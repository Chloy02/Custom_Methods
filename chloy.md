<!-- walnut:generated -->

# chloy — Walnut Project Context

> Auto-generated by the Walnut extension from this project's Walnut context.
> Manual edits will be overwritten on the next link/refresh. To take ownership
> of this file, delete the `walnut:generated` marker on the first line.

**Project:** chloy
**Repository:** Custom_Methods

## Overview

# Project Overview

The **chloy** project is a collection of three independent Python and TypeScript repositories under the same GitHub organization. No project-level description was provided, so the overall purpose must be inferred from the individual repos. The three repos appear to be separate tools rather than a single integrated system: an attendance monitoring pipeline, a fashion recommendation app, and a browser automation library.

The largest repo by far is **Attendance-monitoring-using-Computer-Vision** (~17,474 lines of Python), which is a computer vision system for automated classroom attendance. It consists of a video-processing pipeline (`main.py`) backed by StrongSORT multi-object tracking, FaceNet-based face recognition, and DeepFace emotion detection, a FastAPI backend (`api_server.py`) exposing 7 GET endpoints, and a React/JSX frontend in `frontend/src/`. Data is persisted to a MySQL database via SQLAlchemy. The API surface includes `/attendance`, `/stats`, `/emotions/{student_name}`, `/reports/export`, `/students`, and `/search`. **Fashion_Fusion** (~1,583 lines) is a standalone Streamlit app that classifies uploaded clothing images using a TensorFlow model and returns outfit recommendations, optionally calling a Groq LLM API for natural-language styling advice. **Custom_Methods** (~451 lines of TypeScript) is a plugin library of async browser-automation helpers for a platform called "Walnut", using `@playwright/mcp`.

The primary languages across the project are Python (Attendance-monitoring and Fashion_Fusion) and TypeScript (Custom_Methods). Key frameworks include PyTorch and TensorFlow for ML inference, FastAPI for the HTTP layer, SQLAlchemy for database access, Streamlit for the fashion app UI, and Playwright MCP for browser automation. The only confirmed external service integration is MySQL; the fashion app appears to optionally call a Groq API, and the attendance repo includes a `CloudDeployer` utility targeting AWS RDS, Google Cloud, and Heroku.

## System Architecture

# System Architecture

The three repos in this project do not appear to share a common runtime, API contract, or data store — they are best understood as independent tools that happen to live under the same GitHub organization. No cross-repo imports, shared type definitions, or common authentication layer are evident from the available summaries.

**Attendance-monitoring-using-Computer-Vision** — the HTTP-serving backend + embedded frontend + ML worker
- Owns all 7 FastAPI endpoints: `/` (root), `/attendance`, `/stats`, `/emotions/{student_name}`, `/reports/export`, `/search`, and `/students`.
- `main.py` runs the video-processing loop (StrongSORT tracker → FaceNet face recognition → DeepFace emotion detection) and writes Attendance and EmotionRecord rows to MySQL via the `mysqlDatabase` SQLAlchemy class.
- `api_server.py` serves the React frontend (located in `frontend/src/`) and exposes the REST endpoints that the React app appears to consume.
- `deploy_to_cloud.py` (`CloudDeployer`) handles database migration to AWS RDS, Google Cloud, or Heroku targets.
- The `strongsort/` directory is an embedded copy of the StrongSORT tracker library, not a separate repo.

**Fashion_Fusion** — standalone Streamlit single-page app
- No HTTP API surface; the Streamlit UI in `app.py` is the only user-facing interface.
- Loads a trained TensorFlow model from `models/final_model.h5` at startup.
- Offline training scripts (`train_model.py`, `dataset.py`) are run separately and are not part of the live app.
- No visible connection to the other two repos.

**Custom_Methods** — TypeScript plugin library for the Walnut automation platform
- No web server, no database, no API endpoints.
- All source files export a single async function accepting a `WalnutContext` (e.g., `solveImageCaptcha`, `generatePhoneNumber`, `login`).
- Depends solely on `@playwright/mcp`; execution is driven by the external Walnut host runtime.
- No visible connection to the other two repos.

## Domain

# Domain
Inferred from code only — may be inaccurate for early-stage or generic projects.

The three repos address three distinct problem domains with no evident overlap. **Attendance-monitoring-using-Computer-Vision** is an automated classroom attendance and engagement system. A camera feed is processed frame-by-frame: persons are tracked with StrongSORT, faces are identified via an InceptionResNetV2/FaceNet pipeline, and the resulting identities plus per-student emotion readings (via DeepFace) are stored as Attendance and EmotionRecord rows in MySQL. Educators or administrators access this data through a React dashboard backed by the FastAPI service, where they can filter attendance by student name, view aggregate statistics, browse per-student emotion histories, and export date-ranged reports. A `cheat_det.py` file hints at a secondary exam-proctoring use case, though evidence is limited to the filename.

**Fashion_Fusion** is a fashion item recognition and outfit recommendation tool. A user uploads a clothing image; a TensorFlow classifier (trained on or inspired by the DeepFashion benchmark) identifies the garment category and confidence score. The `FashionRecommender` class then computes outfit compatibility scores and explains pairings, with season-aware logic adjusting suggestions by time of year. An optional Groq LLM integration (`get_groq_suggestions()`) can generate natural-language styling advice.

**Custom_Methods** is a library of reusable browser-automation steps for a platform called Walnut. The functions — CAPTCHA solving, form clearing, phone number generation, and login — suggest the library supports QA or demo workflows that navigate real or simulated web pages presenting bot-detection challenges. There is no domain overlap with the other two repos; this appears to be a utility or tooling repo rather than a product feature.

## Metadata

```json
{
  "project_id": "6985cdc98fc3128f0097c87b",
  "project_name": "chloy",
  "generated_at": "2026-05-07T12:37:15Z",
  "llm_model": "claude-sonnet-4-6",
  "total_repos": 3,
  "total_loc": 19508,
  "language_distribution": {
    "Attendance-monitoring-using-Computer-Vision": 17474,
    "Custom_Methods": 451,
    "Fashion_Fusion": 1583
  },
  "primary_languages": [
    "Python",
    "TypeScript"
  ],
  "all_frameworks": [
    "PyTorch",
    "SQLAlchemy",
    "TensorFlow",
    "numpy",
    "pandas",
    "scikit-learn"
  ],
  "all_external_integrations": [
    "MySQL"
  ],
  "total_api_endpoints": 7,
  "api_frameworks": [
    "fastapi"
  ],
  "api_top_prefixes": {
    "/": 1,
    "/attendance": 1,
    "/emotions/{student_name}": 1,
    "/reports/export": 1,
    "/search": 1,
    "/stats": 1,
    "/students": 1
  },
  "repos": [
    {
      "repo_id": "github_Chloy02/Attendance-monitoring-using-Computer-Vision_1771241289799_k4pm6x7hm",
      "repo_name": "Attendance-monitoring-using-Computer-Vision",
      "primary_language": "Python",
      "architecture_md": "# Architecture\n\nThis is a Python-based computer vision pipeline with a FastAPI backend and a React frontend. The primary language is Python (56 `.py` files out of 107 total), and the system is built around two entry points: `main.py`, which runs the core video-processing loop, and `api_server.py`, which hosts the FastAPI REST service. A secondary optimized variant of the pipeline lives in `main_optimized.py`. The frontend (`frontend/src/`) is a React/JSX application (9 `.jsx` files) that appears to consume the FastAPI endpoints.\n\nThe FastAPI server in `api_server.py` exposes 7 GET endpoints. The largest groups are `/attendance` (fetches Attendance records with optional name filtering and pagination), `/stats` (returns aggregate statistics), and `/emotions/{student_name}` (returns EmotionRecord data per student). A `/reports/export` endpoint accepts date-range parameters and appears to generate downloadable reports. Database access is mediated through SQLAlchemy, backed by a MySQL database, with the `mysqlDatabase` class in `sqlalchemy_database.py` handling all CRUD operations.\n\nThe `strongsort/` directory is a substantial embedded library (~21 model files, plus sort and utils subdirectories) that implements the StrongSORT multi-object tracker. It bundles a large re-identification model zoo (ResNet, OSNet, DenseNet, MobileNetV2, NASNet, SENet, and others as PyTorch `nn.Module` subclasses) and a Kalman-filter-based tracker. The main pipeline in `main.py` calls into this tracker alongside face recognition (InceptionResNetV2, FaceNet via `facenet-pytorch`) and emotion detection (`deepface`). Model weights are stored as `.pt`/`.pth` files committed to the repository. Configuration is loaded from a `.env` file via `python-dotenv`, and `deploy_to_cloud.py` provides a `CloudDeployer` utility that supports AWS RDS, Google Cloud, and Heroku database targets.\n",
      "domain_md": "# Domain\nInferred from code only — may be inaccurate for early-stage or generic projects.\n\nThis project is an automated classroom (or similar venue) attendance monitoring system that uses computer vision instead of manual roll-calls. A camera feed is processed frame-by-frame: persons are detected and tracked using StrongSORT, their faces are recognized using an InceptionResNetV2-based face encoding pipeline, and the resulting identities are recorded as Attendance entries in a MySQL database. Alongside attendance, the system captures per-student emotion data (stored as EmotionRecord rows) using DeepFace, enabling instructors to review not just who was present but also their detected emotional states during a session.\n\nThe likely users are educators or administrators who want a passive, camera-driven record of who attended a class and how engaged they appeared. The FastAPI backend serves this data to a React dashboard, where users can query Attendance records by student name, view aggregated stats, browse per-student EmotionRecord histories, and export date-ranged attendance reports. A `cheat_det.py` entry point suggests the system may also include a secondary use case around detecting suspicious behavior (e.g., cheating during exams), though the signal for this is limited to the file name alone.\n",
      "frameworks": [
        "PyTorch",
        "SQLAlchemy",
        "TensorFlow",
        "numpy",
        "pandas",
        "scikit-learn"
      ],
      "external_integrations": [
        "MySQL"
      ],
      "entry_points": [
        "main.py",
        "app.py"
      ],
      "top_level_dirs": [
        "frontend",
        "strongsort"
      ],
      "total_loc": 17474,
      "api_endpoint_count": 7,
      "api_server_count": 7,
      "api_frameworks": [
        "fastapi"
      ],
      "api_top_prefixes": {
        "/": 1,
        "/attendance": 1,
        "/emotions/{student_name}": 1,
        "/reports/export": 1,
        "/search": 1,
        "/stats": 1,
        "/students": 1
      }
    },
    {
      "repo_id": "github_Chloy02/Fashion_Fusion_1771322166733_zj0infxxv",
      "repo_name": "Fashion_Fusion",
      "primary_language": "Python",
      "architecture_md": "# Architecture\n\nFashion_Fusion is a Streamlit-based Python application — a single-page interactive web app rather than a REST backend or CLI tool. The entry point is `app.py`, which defines the `main()` function that drives the Streamlit UI. The primary ML framework is TensorFlow (\u003e=2.13.0, high confidence), used to load and run a trained image classification model (`load_model()`, `process_predictions()`). Pillow handles image I/O, NumPy and pandas support numerical operations and data wrangling, and `pathlib` manages file paths.\n\nThe codebase is small (~1,583 lines across 10 Python files) and appears to follow a flat layout — all source files sit at the top level with no subdirectory packages. Configuration is stored in JSON files under a `config/` directory, and trained model weights are expected at `models/final_model.h5`. There is no HTTP API surface; the Streamlit app is the only user-facing interface.\n\nTraining and data preparation are handled by separate scripts (`train_model.py`, `dataset.py`, `create_annotations.py`, `verify_dataset.py`) that are run offline rather than as part of the live app. The training loop in `train_model.py` includes hardware-aware throttling (`should_pause_training()` monitors CPU, memory, and temperature thresholds), suggesting it was designed to run on a local machine rather than a managed cloud training service.\n",
      "domain_md": "# Domain\nInferred from code only — may be inaccurate for early-stage or generic projects.\n\nFashion_Fusion appears to be a fashion item recognition and outfit recommendation tool aimed at end users who want styling suggestions. A user uploads a clothing image; the app runs it through a TensorFlow model to classify the garment category and confidence score, then surfaces style recommendations via `get_style_recommendations()` and optionally `get_groq_suggestions()` — the latter suggesting integration with an external LLM API (Groq) for natural-language outfit advice. Season-aware logic (`get_current_season()`) tailors recommendations to the time of year.\n\nThe underlying dataset class is named `DeepFashionDataset`, indicating the model was trained on (or inspired by) the DeepFashion benchmark, a well-known large-scale fashion image dataset. The `FashionRecommender` class in `recommendation_service.py` extends this further by computing outfit compatibility scores between two items (`get_outfit_compatibility_score()`) and explaining why one item was recommended alongside another (`explain_recommendation()`). The likely users are fashion-conscious consumers or developers building a styling assistant.\n",
      "frameworks": [
        "TensorFlow",
        "numpy",
        "pandas"
      ],
      "entry_points": [
        "app.py"
      ],
      "total_loc": 1583
    },
    {
      "repo_id": "github_Chloy02/Custom_Methods_1777363322144_bs3brmw81",
      "repo_name": "Custom_Methods",
      "primary_language": "TypeScript",
      "architecture_md": "# Architecture\n\nThis project is a TypeScript library (or plugin collection) of custom automation methods, likely designed to run within a \"Walnut\" automation platform. There is no detected web framework, backend service, or frontend app — the codebase consists entirely of standalone async TypeScript functions, each accepting a `WalnutContext` parameter. This context-passing pattern strongly suggests these are plug-in methods registered into a host runtime rather than independently executable scripts.\n\nThe sole declared dependency is `@playwright/mcp` (version `^0.0.71`), which is the Playwright Model Context Protocol package. This indicates the methods interact with a browser automation layer — likely controlling or inspecting web pages via Playwright under the hood. No build tooling, test runner, or entry point is explicitly declared in the manifest, so execution is presumably driven by the Walnut host environment that loads and invokes these functions.\n\nAll source files live under the single `walnut-methods/` directory. Each `.ts` file exports one async function named after its file (e.g., `solve_image_captcha.ts` exports `solveImageCaptcha`). There are no API endpoints, database schemas, or HTTP-facing surfaces — this is purely a collection of callable automation routines.\n",
      "domain_md": "# Domain\nInferred from code only — may be inaccurate for early-stage or generic projects.\n\nThis project appears to be a set of custom browser-automation helpers for a platform called \"Walnut.\" The function names — `solveImageCaptcha`, `captchaRedirectToWikipedia`, `clearTextFromObject`, `generatePhoneNumber`, and `login` — suggest the library is designed to assist automated test or demo scenarios that involve CAPTCHA handling, form interaction, and synthetic data generation (e.g., phone numbers).\n\nThe likely users are QA engineers or demo engineers who need reusable, named steps within Walnut-driven automation flows. The CAPTCHA-related methods in particular suggest the workflows navigate real or simulated web pages that present bot-detection challenges, and these custom methods provide pre-built strategies for dealing with them.\n",
      "top_level_dirs": [
        "walnut-methods"
      ],
      "total_loc": 451
    }
  ]
}
```

---
_Source: Walnut code graph / project context. Treat this as background context — verify against the code before relying on it._
